Issues Found
I've analyzed your EcoQuest project and identified 3 critical issues:

1. Non-functional Image Submit (Critical - No XP Saved)
File: page.tsx

The XP is displayed but never saved. In the handleScan() function, after receiving the result, you show +{result.xp} XP but never call addXP(). The points appear temporarily but don't persist to localStorage.

Missing: Need to call addXP(result.xp) after successful API response.

2. XP Bar Calculation Broken at Max Rank
File: page.tsx (lines ~95-97)

For "Gaia Legend", nextThreshold is Infinity, causing division by Infinity → NaN in the progress bar.

3. Conflicting Leveling Definitions
Files: page.tsx vs xp.ts

Two different definitions of the same rank thresholds:

scanner.tsx: Seedling: 100 (threshold needed)
xp.ts: Seedling: min: 0 (minimum XP to have rank)
This inconsistency makes the code confusing and error-prone. Should be one source of truth.